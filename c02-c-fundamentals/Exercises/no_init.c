/* Question 4 */
/* no_init.c */

#include <stdio.h>

int main(void)
{
    int i, j, k;
    float x, y, z;

    printf("i: %d, j: %d, k: %d\n", i, j, k);
    printf("x: %f, y: %f, z: %f\n", x, y, z);

    return 0;
}

// No pattern to the values